import React from "react";
import ReactDOM from "react-dom/client";
import StickerFinderLanding from "./App.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <StickerFinderLanding />
  </React.StrictMode>
);
